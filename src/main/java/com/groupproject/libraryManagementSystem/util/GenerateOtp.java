package com.groupproject.libraryManagementSystem.util;

import lombok.Getter;

import java.util.Random;
import java.util.concurrent.ThreadLocalRandom;
@Getter
public class GenerateOtp {
//    int otp = ThreadLocalRandom.current().nextInt(1000, 10000);
    /**Alternatively I can use the below method but it will delay processing time.

//    Random random = new Random();
//
//    // Generate a random 4-digit OTP
//    int min = 1000;  // Smallest 4-digit number
//    int max = 9999;  // Largest 4-digit number
//    int otp = random.nextInt(max - min + 1) + min;
     **/
}
